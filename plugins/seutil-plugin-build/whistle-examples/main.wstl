import "class:com.essaid.whistle.plugin.seutil.SeUtilPlugin"
import "./functions.wstl"

hello: toUpper("world")

if seutil::fnExists("functions", "functionName", "arg1") then {
    exists: true
    functions::functionName("arg1")
} else {
  exists: false
  // This would be an error since the function doesn't exist.
  // functions::functionName("arg1")
}

